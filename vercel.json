{
  "version": 2,
  "builds": [
    { "src": "main.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/main.py" }
  ],
  "framework": {
    "name": "python",
    "buildCommand": "pip install -r requirements.txt && python main.py"
  }
}
